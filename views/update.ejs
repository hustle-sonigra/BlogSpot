<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog</title>
  <style>
    body {
      margin: 0;
      padding: 20px;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #36d1dc, #5b86e5);
      color: #fff;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .form-container {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(6px);
      border-radius: 15px;
      padding: 30px;
      max-width: 600px;
      width: 100%;
      box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
      text-align: center;
    }

    h1 {
      margin-bottom: 20px;
      font-size: 2rem;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    input[type="text"], textarea {
      padding: 12px;
      border: none;
      border-radius: 10px;
      font-size: 1rem;
      width: 100%;
      outline: none;
    }

    textarea {
      min-height: 150px;
      resize: vertical;
    }

    input[type="submit"] {
      background: #764ba2;
      color: #fff;
      font-weight: bold;
      border: none;
      padding: 12px;
      border-radius: 25px;
      cursor: pointer;
      transition: 0.3s;
    }

    input[type="submit"]:hover {
      opacity: 0.9;
      transform: scale(1.05);
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h1>Update...</h1>
    <form action="/post/display/<%=post._id%>" method="post">
      <input type="text" placeholder="title" name="postTittle" value="<%= post.postTittle %>">
      <textarea placeholder="content" name="postData"><%= post.postData %></textarea>
      <input type="submit" value="Update">
    </form>
  </div>
</body>
</html>
